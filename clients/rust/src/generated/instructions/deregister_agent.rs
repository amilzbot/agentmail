//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshSerialize;
use borsh::BorshDeserialize;

pub const DEREGISTER_AGENT_DISCRIMINATOR: u8 = 5;

/// Accounts.
#[derive(Debug)]
pub struct DeregisterAgent {
      
              
          pub agent_authority: solana_pubkey::Pubkey,
          
              
          pub agent_registry: solana_pubkey::Pubkey,
          
              
          pub agentmail_program: solana_pubkey::Pubkey,
      }

impl DeregisterAgent {
  pub fn instruction(&self) -> solana_instruction::Instruction {
    self.instruction_with_remaining_accounts(&[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::vec_init_then_push)]
  pub fn instruction_with_remaining_accounts(&self, remaining_accounts: &[solana_instruction::AccountMeta]) -> solana_instruction::Instruction {
    let mut accounts = Vec::with_capacity(3+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new(
            self.agent_authority,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            self.agent_registry,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.agentmail_program,
            false
          ));
                      accounts.extend_from_slice(remaining_accounts);
    let data = DeregisterAgentInstructionData::new().try_to_vec().unwrap();
    
    solana_instruction::Instruction {
      program_id: crate::AGENTMAIL_ID,
      accounts,
      data,
    }
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
 pub struct DeregisterAgentInstructionData {
            discriminator: u8,
      }

impl DeregisterAgentInstructionData {
  pub fn new() -> Self {
    Self {
                        discriminator: 5,
                  }
  }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
    borsh::to_vec(self)
  }
  }

impl Default for DeregisterAgentInstructionData {
  fn default() -> Self {
    Self::new()
  }
}



/// Instruction builder for `DeregisterAgent`.
///
/// ### Accounts:
///
                      ///   0. `[writable, signer]` agent_authority
                ///   1. `[writable]` agent_registry
          ///   2. `[]` agentmail_program
#[derive(Clone, Debug, Default)]
pub struct DeregisterAgentBuilder {
            agent_authority: Option<solana_pubkey::Pubkey>,
                agent_registry: Option<solana_pubkey::Pubkey>,
                agentmail_program: Option<solana_pubkey::Pubkey>,
                __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl DeregisterAgentBuilder {
  pub fn new() -> Self {
    Self::default()
  }
            #[inline(always)]
    pub fn agent_authority(&mut self, agent_authority: solana_pubkey::Pubkey) -> &mut Self {
                        self.agent_authority = Some(agent_authority);
                    self
    }
            #[inline(always)]
    pub fn agent_registry(&mut self, agent_registry: solana_pubkey::Pubkey) -> &mut Self {
                        self.agent_registry = Some(agent_registry);
                    self
    }
            #[inline(always)]
    pub fn agentmail_program(&mut self, agentmail_program: solana_pubkey::Pubkey) -> &mut Self {
                        self.agentmail_program = Some(agentmail_program);
                    self
    }
            /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
    self.__remaining_accounts.push(account);
    self
  }
  /// Add additional accounts to the instruction.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[solana_instruction::AccountMeta]) -> &mut Self {
    self.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[allow(clippy::clone_on_copy)]
  pub fn instruction(&self) -> solana_instruction::Instruction {
    let accounts = DeregisterAgent {
                              agent_authority: self.agent_authority.expect("agent_authority is not set"),
                                        agent_registry: self.agent_registry.expect("agent_registry is not set"),
                                        agentmail_program: self.agentmail_program.expect("agentmail_program is not set"),
                      };
    
    accounts.instruction_with_remaining_accounts(&self.__remaining_accounts)
  }
}

  /// `deregister_agent` CPI accounts.
  pub struct DeregisterAgentCpiAccounts<'a, 'b> {
          
                    
              pub agent_authority: &'b solana_account_info::AccountInfo<'a>,
                
                    
              pub agent_registry: &'b solana_account_info::AccountInfo<'a>,
                
                    
              pub agentmail_program: &'b solana_account_info::AccountInfo<'a>,
            }

/// `deregister_agent` CPI instruction.
pub struct DeregisterAgentCpi<'a, 'b> {
  /// The program to invoke.
  pub __program: &'b solana_account_info::AccountInfo<'a>,
      
              
          pub agent_authority: &'b solana_account_info::AccountInfo<'a>,
          
              
          pub agent_registry: &'b solana_account_info::AccountInfo<'a>,
          
              
          pub agentmail_program: &'b solana_account_info::AccountInfo<'a>,
        }

impl<'a, 'b> DeregisterAgentCpi<'a, 'b> {
  pub fn new(
    program: &'b solana_account_info::AccountInfo<'a>,
          accounts: DeregisterAgentCpiAccounts<'a, 'b>,
          ) -> Self {
    Self {
      __program: program,
              agent_authority: accounts.agent_authority,
              agent_registry: accounts.agent_registry,
              agentmail_program: accounts.agentmail_program,
                }
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], &[])
  }
  #[inline(always)]
  pub fn invoke_with_remaining_accounts(&self, remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
  }
  #[inline(always)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed_with_remaining_accounts(
    &self,
    signers_seeds: &[&[&[u8]]],
    remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]
  ) -> solana_program_error::ProgramResult {
    let mut accounts = Vec::with_capacity(3+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new(
            *self.agent_authority.key,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new(
            *self.agent_registry.key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.agentmail_program.key,
            false
          ));
                      remaining_accounts.iter().for_each(|remaining_account| {
      accounts.push(solana_instruction::AccountMeta {
          pubkey: *remaining_account.0.key,
          is_signer: remaining_account.1,
          is_writable: remaining_account.2,
      })
    });
    let data = DeregisterAgentInstructionData::new().try_to_vec().unwrap();
    
    let instruction = solana_instruction::Instruction {
      program_id: crate::AGENTMAIL_ID,
      accounts,
      data,
    };
    let mut account_infos = Vec::with_capacity(4 + remaining_accounts.len());
    account_infos.push(self.__program.clone());
                  account_infos.push(self.agent_authority.clone());
                        account_infos.push(self.agent_registry.clone());
                        account_infos.push(self.agentmail_program.clone());
              remaining_accounts.iter().for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

    if signers_seeds.is_empty() {
      solana_cpi::invoke(&instruction, &account_infos)
    } else {
      solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
    }
  }
}

/// Instruction builder for `DeregisterAgent` via CPI.
///
/// ### Accounts:
///
                      ///   0. `[writable, signer]` agent_authority
                ///   1. `[writable]` agent_registry
          ///   2. `[]` agentmail_program
#[derive(Clone, Debug)]
pub struct DeregisterAgentCpiBuilder<'a, 'b> {
  instruction: Box<DeregisterAgentCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> DeregisterAgentCpiBuilder<'a, 'b> {
  pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
    let instruction = Box::new(DeregisterAgentCpiBuilderInstruction {
      __program: program,
              agent_authority: None,
              agent_registry: None,
              agentmail_program: None,
                                __remaining_accounts: Vec::new(),
    });
    Self { instruction }
  }
      #[inline(always)]
    pub fn agent_authority(&mut self, agent_authority: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.agent_authority = Some(agent_authority);
                    self
    }
      #[inline(always)]
    pub fn agent_registry(&mut self, agent_registry: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.agent_registry = Some(agent_registry);
                    self
    }
      #[inline(always)]
    pub fn agentmail_program(&mut self, agentmail_program: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.agentmail_program = Some(agentmail_program);
                    self
    }
            /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: &'b solana_account_info::AccountInfo<'a>, is_writable: bool, is_signer: bool) -> &mut Self {
    self.instruction.__remaining_accounts.push((account, is_writable, is_signer));
    self
  }
  /// Add additional accounts to the instruction.
  ///
  /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
  /// and a `bool` indicating whether the account is a signer or not.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> &mut Self {
    self.instruction.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed(&[])
  }
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let instruction = DeregisterAgentCpi {
        __program: self.instruction.__program,
                  
          agent_authority: self.instruction.agent_authority.expect("agent_authority is not set"),
                  
          agent_registry: self.instruction.agent_registry.expect("agent_registry is not set"),
                  
          agentmail_program: self.instruction.agentmail_program.expect("agentmail_program is not set"),
                    };
    instruction.invoke_signed_with_remaining_accounts(signers_seeds, &self.instruction.__remaining_accounts)
  }
}

#[derive(Clone, Debug)]
struct DeregisterAgentCpiBuilderInstruction<'a, 'b> {
  __program: &'b solana_account_info::AccountInfo<'a>,
            agent_authority: Option<&'b solana_account_info::AccountInfo<'a>>,
                agent_registry: Option<&'b solana_account_info::AccountInfo<'a>>,
                agentmail_program: Option<&'b solana_account_info::AccountInfo<'a>>,
                /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
  __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}

